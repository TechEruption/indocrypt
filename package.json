{
  "name": "semantic-oracle-context-binding-poc",
  "version": "1.0.0",
  "private": true,
  "description": "Research PoC for semantic-aware cryptographic context binding in blockchain oracles",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "experiment": "hardhat run scripts/gasExperiment.ts",
    "sepolia:metrics": "hardhat run scripts/sepoliaMetrics.ts --network sepolia",
    "deploy": "hardhat run scripts/deploy.ts",
    "node": "hardhat node",
    "ui": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10",
    "typescript": "^5.6.3",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2"
  }
}
